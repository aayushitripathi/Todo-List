
// create task constructor function
add properties
create UI for input
on add input get display on screen

CREATE OPERATION
1.create task object
2.save the task object in array
3.display the task on the DOM
<div id="task.taskId">
<p>task.taskId</p>
<p>task.description</p>
<p>task.createdOn</p>
<button>delete</button>
</div>

DELETE OPERATION
1.input should be task.taskId
2.remove the corresponding id